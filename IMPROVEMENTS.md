# Kyvorix improvements applied

- Fixed the WebP to JPG CSS transition typo.
- Fixed PNG compressor behavior so it keeps/downloads the original when browser re-encoding produces a file that is not smaller.
- Added homepage tool search.
- Added recently used tools using localStorage.
- Added quick-access privacy/speed trust signals.
- Improved homepage interaction polish and responsive styling.
- Added local recent-tool tracking across individual tool pages.

## Still required before production
Replace every `https://YOUR-DOMAIN.com` canonical/Open Graph/schema URL with the final production domain.
